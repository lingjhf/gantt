import 'package:flutter/material.dart';

class ConnectLineData {
  const ConnectLineData({
    required this.startOffset,
    required this.endOffset,
  });
  final Offset startOffset;
  final Offset endOffset;
}
